{
  "name": "@pguard/e2e",
  "private": true,
  "version": "0.0.0",
  "description": "pguard v2 end-to-end tests — Playwright (web-admin) + Patrol (mobile, under ./mobile).",
  "scripts": {
    "test:web": "playwright test",
    "test:web:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^20.19.0"
  }
}
