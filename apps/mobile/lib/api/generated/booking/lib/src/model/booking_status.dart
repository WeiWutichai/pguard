//
// AUTO-GENERATED FILE, DO NOT MODIFY!
//

// ignore_for_file: unused_element
import 'package:built_collection/built_collection.dart';
import 'package:built_value/built_value.dart';
import 'package:built_value/serializer.dart';

part 'booking_status.g.dart';

class BookingStatus extends EnumClass {

  /// Booking lifecycle status. See the state machine in src/domain/state.rs.  **Scheduler-driven closures (QA #25).** `arrived` is not an indefinite state: 30 minutes past `scheduled_at + hours` the background sweep closes a booking still sitting there (the guard never pressed จบงาน, or the customer sent them back), because the customer's PRE-PAY would otherwise be held with no completion event to reconcile it. It closes to `completed` when work was started AND attested by at least one check-in — billed on the PAID WINDOW (`min(scheduled_at + hours, now) − work_started_at`, floored at zero), so the grace is never billed, a late sweep tick never inflates the bill, and a guard who started late is credited only the part of the window they were on site. It closes to `cancelled` with `cancellation_reason = system_not_started` (no fee → payment full-refunds) when the guard marked themselves on-site but never started, since there is no worked time to bill at all. A job started but never attested is left untouched for a human — billing it would pay a guard who bypassed the check-in rule, cancelling it would write off pay that may just be a failed photo upload.
  @BuiltValueEnumConst(wireName: r'requested')
  static const BookingStatus requested = _$requested;
  /// Booking lifecycle status. See the state machine in src/domain/state.rs.  **Scheduler-driven closures (QA #25).** `arrived` is not an indefinite state: 30 minutes past `scheduled_at + hours` the background sweep closes a booking still sitting there (the guard never pressed จบงาน, or the customer sent them back), because the customer's PRE-PAY would otherwise be held with no completion event to reconcile it. It closes to `completed` when work was started AND attested by at least one check-in — billed on the PAID WINDOW (`min(scheduled_at + hours, now) − work_started_at`, floored at zero), so the grace is never billed, a late sweep tick never inflates the bill, and a guard who started late is credited only the part of the window they were on site. It closes to `cancelled` with `cancellation_reason = system_not_started` (no fee → payment full-refunds) when the guard marked themselves on-site but never started, since there is no worked time to bill at all. A job started but never attested is left untouched for a human — billing it would pay a guard who bypassed the check-in rule, cancelling it would write off pay that may just be a failed photo upload.
  @BuiltValueEnumConst(wireName: r'accepted')
  static const BookingStatus accepted = _$accepted;
  /// Booking lifecycle status. See the state machine in src/domain/state.rs.  **Scheduler-driven closures (QA #25).** `arrived` is not an indefinite state: 30 minutes past `scheduled_at + hours` the background sweep closes a booking still sitting there (the guard never pressed จบงาน, or the customer sent them back), because the customer's PRE-PAY would otherwise be held with no completion event to reconcile it. It closes to `completed` when work was started AND attested by at least one check-in — billed on the PAID WINDOW (`min(scheduled_at + hours, now) − work_started_at`, floored at zero), so the grace is never billed, a late sweep tick never inflates the bill, and a guard who started late is credited only the part of the window they were on site. It closes to `cancelled` with `cancellation_reason = system_not_started` (no fee → payment full-refunds) when the guard marked themselves on-site but never started, since there is no worked time to bill at all. A job started but never attested is left untouched for a human — billing it would pay a guard who bypassed the check-in rule, cancelling it would write off pay that may just be a failed photo upload.
  @BuiltValueEnumConst(wireName: r'declined')
  static const BookingStatus declined = _$declined;
  /// Booking lifecycle status. See the state machine in src/domain/state.rs.  **Scheduler-driven closures (QA #25).** `arrived` is not an indefinite state: 30 minutes past `scheduled_at + hours` the background sweep closes a booking still sitting there (the guard never pressed จบงาน, or the customer sent them back), because the customer's PRE-PAY would otherwise be held with no completion event to reconcile it. It closes to `completed` when work was started AND attested by at least one check-in — billed on the PAID WINDOW (`min(scheduled_at + hours, now) − work_started_at`, floored at zero), so the grace is never billed, a late sweep tick never inflates the bill, and a guard who started late is credited only the part of the window they were on site. It closes to `cancelled` with `cancellation_reason = system_not_started` (no fee → payment full-refunds) when the guard marked themselves on-site but never started, since there is no worked time to bill at all. A job started but never attested is left untouched for a human — billing it would pay a guard who bypassed the check-in rule, cancelling it would write off pay that may just be a failed photo upload.
  @BuiltValueEnumConst(wireName: r'en_route')
  static const BookingStatus enRoute = _$enRoute;
  /// Booking lifecycle status. See the state machine in src/domain/state.rs.  **Scheduler-driven closures (QA #25).** `arrived` is not an indefinite state: 30 minutes past `scheduled_at + hours` the background sweep closes a booking still sitting there (the guard never pressed จบงาน, or the customer sent them back), because the customer's PRE-PAY would otherwise be held with no completion event to reconcile it. It closes to `completed` when work was started AND attested by at least one check-in — billed on the PAID WINDOW (`min(scheduled_at + hours, now) − work_started_at`, floored at zero), so the grace is never billed, a late sweep tick never inflates the bill, and a guard who started late is credited only the part of the window they were on site. It closes to `cancelled` with `cancellation_reason = system_not_started` (no fee → payment full-refunds) when the guard marked themselves on-site but never started, since there is no worked time to bill at all. A job started but never attested is left untouched for a human — billing it would pay a guard who bypassed the check-in rule, cancelling it would write off pay that may just be a failed photo upload.
  @BuiltValueEnumConst(wireName: r'arrived')
  static const BookingStatus arrived = _$arrived;
  /// Booking lifecycle status. See the state machine in src/domain/state.rs.  **Scheduler-driven closures (QA #25).** `arrived` is not an indefinite state: 30 minutes past `scheduled_at + hours` the background sweep closes a booking still sitting there (the guard never pressed จบงาน, or the customer sent them back), because the customer's PRE-PAY would otherwise be held with no completion event to reconcile it. It closes to `completed` when work was started AND attested by at least one check-in — billed on the PAID WINDOW (`min(scheduled_at + hours, now) − work_started_at`, floored at zero), so the grace is never billed, a late sweep tick never inflates the bill, and a guard who started late is credited only the part of the window they were on site. It closes to `cancelled` with `cancellation_reason = system_not_started` (no fee → payment full-refunds) when the guard marked themselves on-site but never started, since there is no worked time to bill at all. A job started but never attested is left untouched for a human — billing it would pay a guard who bypassed the check-in rule, cancelling it would write off pay that may just be a failed photo upload.
  @BuiltValueEnumConst(wireName: r'pending_completion')
  static const BookingStatus pendingCompletion = _$pendingCompletion;
  /// Booking lifecycle status. See the state machine in src/domain/state.rs.  **Scheduler-driven closures (QA #25).** `arrived` is not an indefinite state: 30 minutes past `scheduled_at + hours` the background sweep closes a booking still sitting there (the guard never pressed จบงาน, or the customer sent them back), because the customer's PRE-PAY would otherwise be held with no completion event to reconcile it. It closes to `completed` when work was started AND attested by at least one check-in — billed on the PAID WINDOW (`min(scheduled_at + hours, now) − work_started_at`, floored at zero), so the grace is never billed, a late sweep tick never inflates the bill, and a guard who started late is credited only the part of the window they were on site. It closes to `cancelled` with `cancellation_reason = system_not_started` (no fee → payment full-refunds) when the guard marked themselves on-site but never started, since there is no worked time to bill at all. A job started but never attested is left untouched for a human — billing it would pay a guard who bypassed the check-in rule, cancelling it would write off pay that may just be a failed photo upload.
  @BuiltValueEnumConst(wireName: r'completed')
  static const BookingStatus completed = _$completed;
  /// Booking lifecycle status. See the state machine in src/domain/state.rs.  **Scheduler-driven closures (QA #25).** `arrived` is not an indefinite state: 30 minutes past `scheduled_at + hours` the background sweep closes a booking still sitting there (the guard never pressed จบงาน, or the customer sent them back), because the customer's PRE-PAY would otherwise be held with no completion event to reconcile it. It closes to `completed` when work was started AND attested by at least one check-in — billed on the PAID WINDOW (`min(scheduled_at + hours, now) − work_started_at`, floored at zero), so the grace is never billed, a late sweep tick never inflates the bill, and a guard who started late is credited only the part of the window they were on site. It closes to `cancelled` with `cancellation_reason = system_not_started` (no fee → payment full-refunds) when the guard marked themselves on-site but never started, since there is no worked time to bill at all. A job started but never attested is left untouched for a human — billing it would pay a guard who bypassed the check-in rule, cancelling it would write off pay that may just be a failed photo upload.
  @BuiltValueEnumConst(wireName: r'cancelled')
  static const BookingStatus cancelled = _$cancelled;

  static Serializer<BookingStatus> get serializer => _$bookingStatusSerializer;

  const BookingStatus._(String name): super(name);

  static BuiltSet<BookingStatus> get values => _$values;
  static BookingStatus valueOf(String name) => _$valueOf(name);
}

/// Optionally, enum_class can generate a mixin to go with your enum for use
/// with Angular. It exposes your enum constants as getters. So, if you mix it
/// in to your Dart component class, the values become available to the
/// corresponding Angular template.
///
/// Trigger mixin generation by writing a line like this one next to your enum.
abstract class BookingStatusMixin = Object with _$BookingStatusMixin;

