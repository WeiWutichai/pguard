<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ───────────────────────────────────────────────────────────────────────────────
         Permissions for the pguard feature set (declarations only; runtime requests are made in
         Dart by permission_handler / the plugins). See apps/mobile/PLATFORM_PERMISSIONS.md.
         No android:usesCleartextTraffic — staging is https; cleartext stays blocked (API 28+ default).
         ─────────────────────────────────────────────────────────────────────────────── -->

    <!-- Networking (REST + booking-status/presence/chat/call WebSockets). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Camera — guard check-in photos + chat image attachments (image_picker) and WebRTC video. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- WebRTC voice/video calling (flutter_webrtc) — mic + audio routing + screen-off audio. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Audio routing to Bluetooth headsets during a call. Legacy BLUETOOTH is implied ≤ API 30;
         API 31+ uses the runtime BLUETOOTH_CONNECT permission (requested via permission_handler). -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Foreground GPS — guard online / presence GPS ingest. (The real fix needs the geolocator
         plugin, a documented future dependency in lib/core/location/location_service.dart; the
         declaration is added now so the feature works the moment that plugin lands.) Fine location
         on API 31+ also requires the coarse declaration. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Keep the GPS uplink streaming while the guard is online with the screen off / app
         backgrounded: geolocator runs a foreground LOCATION service (its <service> with
         foregroundServiceType="location" is merged from geolocator_android). FOREGROUND_SERVICE is
         API 28+; FOREGROUND_SERVICE_LOCATION is required on API 34+ (Android 14). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- FCM push (incoming-call ring). POST_NOTIFICATIONS is the runtime permission on API 33+;
         the FirebaseMessagingService is merged from the firebase_messaging plugin. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Biometric unlock (local_auth) — optional fast path over the PIN gate. USE_BIOMETRIC
         covers API 28+; USE_FINGERPRINT is the legacy fallback for API ≤ 28. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" android:maxSdkVersion="28" />

    <!-- Camera is used opportunistically (check-in / calls), never strictly required to install. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:label="pguard"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- FCM fallback channel: a data/notification push that does NOT set its own channel_id is
             auto-displayed on THIS channel. The server tags pushes with channel_id="pguard_alerts_v2"
             (services/notification/src/fcm.rs) and the app registers a HIGH-importance, sound+vibrate
             "pguard_alerts_v2" channel at startup (core/network/notification_channel.dart), so this
             guarantees the same channel is used even for a push that omits it — no silent low-
             importance fallback. The "_v2" id cache-busts the old silent "default" channel, whose
             importance+sound Android froze at first creation. This value MUST match both. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="pguard_alerts_v2" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
