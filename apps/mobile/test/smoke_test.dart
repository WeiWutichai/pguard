// pguard v2 scaffold stub — trivial smoke test to confirm the test harness runs.
// See ../CLAUDE.md "Flutter (mobile)" — pure logic must be testable without widgets.
// TODO: replace with real controller unit tests (BookingController, proration, etc.).

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('smoke: arithmetic sanity', () {
    expect(1 + 1, equals(2));
  });
}
