import { ComingSoonPage } from "@/components/coming-soon-page";

/** Hi-fi screen not rebuilt yet — placeholder so the shell nav has no dead links. */
export default function Page() {
  return <ComingSoonPage titleKey="nav.chat" />;
}
