// pguard v2 scaffold stub — admin landing placeholder.
// TODO(CLAUDE.md › Service map › apps/web-admin): build out the admin dashboard
// (guard onboarding, payments/refunds, operations monitoring). Use the generated
// TS client from contracts/openapi for all data fetching (src/api/README.md).
// TODO(CLAUDE.md › docs/ROLE_MATRIX.md): gate routes/actions by role.
export default function HomePage() {
  return (
    <main>
      <h1>pguard admin</h1>
      <p>v2 scaffold — replace with the admin dashboard.</p>
    </main>
  );
}
