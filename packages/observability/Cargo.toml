[package]
name = "observability"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
tracing = { workspace = true }
tracing-subscriber = { workspace = true }

# NOTE: OpenTelemetry OTLP export (opentelemetry / opentelemetry-otlp /
# tracing-opentelemetry) is declared in the workspace and will be wired here in a
# follow-up so `init_telemetry` exports spans to the OTel collector → Tempo.
# Kept out of the scaffold build to avoid pulling the full OTel tree before it is used.
