# pguard — Deep Review (2026-06-20)

Multi-agent review of all 14 areas, every blocker/high/medium adversarially verified. Stats: {"high": 4, "medium": 13, "low": 37, "nit": 2} (0 dismissed).

Status legend: ☐ open · ☑ fixed (link PR). HIGH+MEDIUM are being fixed; LOW/nit are backlog.

## 🔴 HIGH (4)

- ☐ **[HIGH/profile/observability] Admin reads of guard ID-document images, avatar, and document-expiries are not recorded in the PDPA §30 access_audit**
  - `/Users/nest/Documents/pguard/services/profile/src/api/mod.rs` get_guard_document 537-559; get_guard_avatar 690-706; list_guard_document_expiries 572-591
  - Why: record_access is called on only 4 paths (api/mod.rs:494, 619, 831, 858): admin doc UPLOAD (write), admin set-expiry (write), admin_list_guard_profiles, admin_list_customer_profiles. But the single-resource admin READS are NOT audited: get_guard_document (an admin pulls a presigned URL to a guard's actual id_card / criminal_check / driver_license / passbook image — the single highest-PII surface in the system), get_guard_avatar, and list_guard_document_expiries. Migration 0002_access_audit.sql explicitly states the trail exists for 'name / DOB / address / bank account / DOCUMENT REFERENCES' and PDPA §30 expects a record of WHO accessed personal data. An admin can therefore retrieve and view any guard's ID-card scan with no §30 record at all, defeating the audit control precisely where it matters most. (The owner reading their own doc need not be audited, but the admin-reading-someone-else's branch must be.)
  - Fix: In get_guard_document / get_guard_avatar / list_guard_document_expiries, when user.role == ROLE_ADMIN && user.user_id != user_id, call repo::record_access(state.db(), user.user_id, "admin_read_guard_document"/"admin_read_guard_avatar"/"admin_list_document_expiries", Some(&format!("{user_id}/{document_type}"))) before returning the presigned URL — fail-loud, mirroring the existing admin_upload_guard_document audit at line 494.
  - Verifier: Verified against the cited code. `get_guard_document` (api/mod.rs:537-559), `get_guard_avatar` (690-706), and `list_guard_document_expiries` (572-591) all use an owner-or-admin auth check (`if user.role != ROLE_ADMIN && user.user_id != user_id { Forbidden }` at lines 544/695/577) that explicitly permits an admin to read another guard's resource, yet none of the three calls `repo::record_access`. By contrast `record_access` IS invoked on exactly the 4 paths the finding names: admin doc upload (49

- ☐ **[HIGH/chat/security] POST /conversations trusts client-supplied participants/request_id with no booking validation — fabricated conversations + display-data injection + read-only bypass**
  - `services/chat/src/api/mod.rs` 62-75 (create_conversation) + repo/mod.rs:86-121
  - Why: The only authorization on conversation creation is `!req.participants.iter().any(|p| p.user_id == user.user_id)` (api/mod.rs:68): the caller must merely list THEMSELVES somewhere in the participants array. Nothing verifies (a) that `request_id` is a real booking, (b) that the caller is actually on that booking, (c) that the claimed counterpart `user_id` is the real other party, or (d) that the participant role the caller claims for themselves matches their token role. The route is a normal Api-tier client endpoint (gateway routing.rs:374-377, reachable by any AuthUser; the mobile client builds the body in chat_launcher.dart:41 — client-supplied request_id, participants, roles, display_name/avatar_url, request_status). Consequences, all reachable by a normal customer/guard token: (1) Fabricate a conversation between yourself and ANY user_id you can guess/learn (their guard/customer id is visible in the booking flow) for a request_id you have no relationship to, then message them — chat never checks the booking pairs you. (2) The counterpart's `display_name`/`avatar_url` are denormalized straight from the attacker's body and surfaced to the victim in their enriched list (EnrichedConversation), i.e. arbitrary display-text/URL injection into another user's chat list. (3) `request_status` is attacker-chosen at create (repo binds req.request_status verbatim): for an already-completed booking, create a fresh conversation row with request_status:'accepted' and the read-only gate (domain::is_writable) treats it as OPEN — booking never re-pushes a terminal status for a row that didn't exist when the lifecycle event fired, so the conversation stays writable forever. This is the v2 architecture intent inverted: CLAUDE.md says cross-service derived state comes 'via API (or events)', but chat takes the booking-derived participant graph straight from the untrusted client.
  - Fix: Either (preferred) make conversation creation server-driven: have booking create the conversation over a service-JWT'd /internal endpoint (or have chat consume pguard.events.booking.job_accepted and build participants from the signed event payload), so participants/roles/request_id all originate from booking, not the client. If client-initiated creation must stay, call booking's API (service-JWT) to verify the caller is a participant of request_id and to fetch the authoritative counterpart id+role+status, and force the caller's own participant role to equal their token role. Reject any participant whose identity/role wasn't confirmed by booking.
  - Verifier: VERIFIED REAL. The only authz on POST /conversations is api/mod.rs:68 `!req.participants.iter().any(|p| p.user_id == user.user_id)` — the caller must merely list themselves in the array. repo::create_conversation (mod.rs:86-121) binds req.request_id, req.request_status, and each participant's user_id/role/display_name/avatar_url verbatim with no validation that (a) request_id is a real booking, (b) the caller is on it, (c) the counterpart id/role is authentic, or (d) the caller's claimed role ma

- ☐ **[HIGH/chat/correctness] WS room-gate is seeded once at connect — a recipient silently drops real-time messages for any conversation created/added after they connected**
  - `services/chat/src/api/ws.rs` 83-89 (prefetch) + 167 (room gate); re-auth tick 178-193 never refreshes
  - Why: On WS open, a non-admin's `authorized` set is prefetched once from participant_conversation_ids (ws.rs:84-88). The outbound broadcast path drops any frame whose conversation_id is not in `authorized` (ws.rs:167 `!authorized.contains(&out.conversation_id) => continue`). The set only grows when THIS user successfully SENDS into a room (ws.rs:128) — never for rooms where the user is only the receiver. The 60s re-auth tick (ws.rs:178) re-validates the token but does NOT re-prefetch authorized rooms. Concrete failure: a guard's WS connects; later a customer creates the booking conversation (lazy creation in chat_launcher) and sends the first message. The guard's `authorized` was snapshotted before that conversation existed, so the guard's session silently `continue`s past the broadcast and the guard receives nothing in real time. The sender-side direct echo (ws.rs:131-138) only reaches the sender, not the peer. The message is persisted and the chat.message_sent notification still fires, so this is not data loss, but the core real-time delivery promise is broken for the recipient of every newly-created conversation until they reconnect (or themselves send into it). Because conversations are created lazily per booking, this is a common path, not a corner case.
  - Fix: Refresh the authorized-rooms set on a cadence and/or just-in-time: when an inbound broadcast arrives for a conversation_id not in `authorized`, do a cheap repo::is_participant check (cache the negative result briefly) before dropping, OR re-run participant_conversation_ids on each re-auth tick, OR subscribe each session only to its own rooms and re-subscribe when membership changes. The cheapest correct fix is the on-miss is_participant verification so genuine members are never starved while non-members are still gated.
  - Verifier: VERIFIED against services/chat/src/api/ws.rs and repo/mod.rs. The `authorized` HashSet is seeded exactly once at connect (ws.rs:85-88 via repo::participant_conversation_ids) and mutated in only one other place — `authorized.insert(cid)` at ws.rs:128, which runs ONLY after THIS user successfully SENDS into a room. The outbound broadcast path drops any frame whose conversation_id is absent from `authorized` (ws.rs:167 `if !is_admin && !authorized.contains(&out.conversation_id) { continue; }`). The

- ☐ **[HIGH/mobile/correctness] Check-in photo upload declares MIME from file extension, not magic bytes — server rejects re-encoded captures**
  - `apps/mobile/lib/core/network/check_in_service.dart` 72-95
  - Why: ApiCheckInService.submit derives the declared Content-Type from the file path extension via `ChatMime.fromPath(photo.path)` and sends it as `contentType: DioMediaType.parse(mime)`. The server (per the contract comment at lines 16-23 and mirrored in services/profile detect_image_mime) magic-byte-validates AND requires the declared MIME to match the actual bytes. The photo comes from ImagePickerPhotoCaptureService.capture(), which calls pickImage with `imageQuality: 80, maxWidth: 2000` — that forces image_picker to RE-ENCODE the image to JPEG while keeping the original source filename. So a camera/gallery image whose source name ends in .png/.webp/.heic produces JPEG bytes declared as image/png (or null→rejected for .heic, which ChatMime doesn't map). The server then 4xx-rejects on the MIME-vs-bytes mismatch, and the hourly check-in — the guard's safety-critical proof-of-presence — fails. This is exactly the scenario the OTHER three upload paths explicitly guard against: guard_documents_controller.dart:213-217, guard_avatar_controller.dart:57-59, and registration_controller.dart:393-405 each read the first 12 bytes and call _detectImageMime (magic bytes), with comments stating 'image_picker may re-encode to JPEG while keeping the source extension, so the extension can't be trusted.' The check-in path is the lone exception. The chat path is safe because ImagePickerChatMediaPicker captures the platform-reported file.mimeType first (chat_media_picker.dart:46), but CapturedPhoto carries only path+sizeBytes and discards the picker's mimeType. The existing test (test/controllers/check_in_service_test.dart) only exercises a .jpg file (extension matches content) and a .mov (rejected), so the re-encode mismatch is untested.
  - Fix: Reuse the magic-byte detector already implemented three times in this repo. Either: (a) in ApiCheckInService.submit, read the first 12 bytes of photo.path and detect the MIME from magic bytes (default image/jpeg, reject null as non-image) instead of ChatMime.fromPath; or (b) capture and carry the picker's file.mimeType through CapturedPhoto and prefer it. Best: extract the shared `detectImageMime(List<int>)` into one util (the controllers already note 'a shared image-upload util is a small follow-up') and use it in the check-in path too. Add a test with a .png-named file containing JPEG magic bytes asserting the declared Content-Type is image/jpeg.
  - Verifier: Verified against the actual code. In apps/mobile/lib/core/network/check_in_service.dart:72-94, ApiCheckInService.submit derives the declared Content-Type from the file EXTENSION via ChatMime.fromPath(photo.path) and sends it as DioMediaType.parse(mime). The server-side enforcement the finding cites is real and lives at the very endpoint this path hits: services/booking/src/domain/progress.rs:85-90, validate_photo_upload returns AppError::BadRequest ("MIME mismatch: declared X but content is Y") 

## 🟠 MEDIUM (13)

- ☐ **[MEDIUM/identity/error-handling] Single-use OTP token is consumed (GETDEL) before the UPSERT + profile-token write, so any failure after that point strands the user mid-registration**
  - `services/identity/src/api/mod.rs` 136-165
  - Why: In register() the phone-verify jti is atomically claimed via GETDEL at step 4 (api/mod.rs:136-145), BEFORE upsert_pending_user (step 5, line 149) and the profile_token Redis SET_EX (step 6, lines 163-165). If the UPSERT fails (transient DB error) or the profile_token Redis write fails, the handler returns 500 — but the phone-verify jti has already been deleted from Redis. The client's retry resends the same phone_verified_token, whose jti is now gone, yielding a 400 'invalid, expired, or already used'. The user is stuck and must restart the whole OTP flow. The ordering fails safe (toward re-verification, never toward unauthorized access), so this is a UX/robustness defect, not a security hole — but it is a real, reproducible dead-end on any post-GETDEL failure.
  - Fix: Either (a) perform the UPSERT first and GETDEL-consume the phone-verify jti only after the account row is committed (accepting a tiny double-register race that ON CONFLICT WHERE pending already absorbs), or (b) wrap the post-GETDEL work so a downstream failure re-stores the jti as 'valid' before returning the error, so a retry can succeed.
  - Verifier: Verified against services/identity/src/api/mod.rs:136-165 and repo/mod.rs:124-159. The single-use phone-verify marker is destructively consumed via GETDEL at lines 137-140 (atomic read+delete of phone_verify_jti:{jti}) BEFORE upsert_pending_user (line 150) and the profile_jti set_ex (lines 163-165). Both later steps are fallible: upsert_pending_user propagates transient sqlx errors via .await? (repo/mod.rs:152), and the Redis set_ex propagates via .await? (line 165). Either failure returns 500 w

- ☐ **[MEDIUM/identity/observability] Force-revoke-all of ACCESS tokens depends entirely on a best-effort Redis marker write with no DB-backed reconciliation**
  - `services/identity/src/state.rs` 93-102
  - Why: revoke_all / soft_delete_and_redact bump token_revocation_version in Postgres (source of truth) and revoke refresh families transactionally, then call mark_user_revoked, which writes user_trv:{id} to Redis best-effort and only logs a warning on failure (state.rs:93-102). authenticate_token (auth.rs:310-317) and the gateway BOTH enforce access-token revocation SOLELY by reading user_trv from Redis, defaulting to 0 when the key is absent — they never fall back to the DB trv. Therefore, if the marker write fails (Redis hiccup at the moment of a compromise-triggered revoke), every outstanding access token for that user remains honored for its full TTL (up to 15 min) even though the user was just force-revoked. The blast radius is bounded (refresh families are revoked, so the session cannot be renewed, and access tokens expire in ≤15 min), which is why this is medium not high — but for the specific use case this gate exists for (account compromise / incident response), a silently-dropped marker is a real soft-failure with no retry or alarm beyond a warn log.
  - Fix: Make the marker write durable/observable on the revoke path: retry with bounded backoff, emit a metric/alert on persistent failure, and/or have authenticate_token fall back to (or periodically reconcile against) the DB trv when the Redis key is absent so a dropped marker cannot silently re-validate revoked access tokens.
  - Verifier: Verified against exact code. mark_user_revoked (services/identity/src/state.rs:93-102) writes user_trv:{id} to Redis best-effort, swallowing any error with only a warn! and returning (). The DB is the source of truth: revoke_all (repo/mod.rs:463-513) transactionally bumps token_revocation_version and revokes all refresh families. But the access-token revocation gate reads ONLY Redis: authenticate_token (packages/shared-rust/src/auth.rs:310-314) and gateway validate (services/api-gateway/src/auth

- ☐ **[MEDIUM/otp/error-handling] SMS-gateway failure burns the user's daily/cooldown budget and leaves an orphan code — transient errors become a self-DoS**
  - `services/otp/src/api/mod.rs` 114-176 (esp. 169)
  - Why: In api/mod.rs::request the side effects are committed BEFORE delivery: step 4 sets otp_rate cooldown (line 116), step 5 INCRs otp_daily (line 130), step 7 stores the hashed code (line 164), and only step 8 sends SMS via state.sms.send(...).await? (line 169). When the gateway returns Err — a timeout, HTTP error, or a business failure like INET code '08' Insufficient SMS Credits / '13' User disabled, all mapped to AppError::Internal in sms.rs:138-161 — the handler returns 500 but the daily counter stays incremented, the 60s cooldown stays set, and a live OTP row exists in otp.otp_codes that the user never received. Because locked/cooled retries are gated at steps 3-4 and don't re-increment, the user is throttled (60s cooldown) AND has permanently lost one of their daily_otp_limit (10) slots per failed attempt. A sustained gateway outage or a credit-exhausted account silently walks legitimate users toward the burst/admin lockout while delivering zero codes — i.e. the failure mode of the SMS provider is converted into an account-level lockout the user cannot distinguish from normal abuse-control.
  - Fix: Send the SMS BEFORE consuming quota, or compensate on failure. Simplest: wrap step 8 so that on Err you DECR otp_daily and DEL otp_rate (and optionally mark the just-stored row used) before propagating, so a delivery failure does not consume the user's budget. Alternatively reorder to generate+send first and only INCR the daily counter after a confirmed send. Keep the outward error generic.
  - Verifier: VERIFIED REAL. In services/otp/src/api/mod.rs::request, the abuse-control side effects are all committed before SMS delivery: step 4 sets the 60s cooldown via SET NX EX (lines 116-123), step 5 INCRs otp_daily (line 130), step 7 stores the hashed code via repo::store_code (line 164), and only then step 8 calls state.sms.send(...).await? (line 169). The `?` propagates any Err as a 500 with no compensation/rollback — confirmed there is no catch, retry, or outbox. sms.rs maps gateway failures (reqwe

- ☐ **[MEDIUM/profile/data-integrity] Account number is silently corrupted by a read-modify-write of the masked value (non-coalescing upsert + masking)**
  - `/Users/nest/Documents/pguard/services/profile/src/repo/mod.rs` 297-298 (upsert ON CONFLICT) and 343 (PUT update); read mask at api/mod.rs:264-267
  - Why: GET /profile/me masks account_number to its last 4 (mask_guard_response → '******7890'). Both POST upsert_guard_profile (ON CONFLICT DO UPDATE SET account_number = EXCLUDED.account_number) and PUT update_guard_profile (account_number = $8) write the request value UNCONDITIONALLY — no COALESCE, no 'keep existing if unchanged'. A naive client (or the web form / mobile edit screen) that loads /profile/me, lets the user edit other fields, and PUTs the whole object back will re-send the MASKED string. validate_text only checks length (MAX_ACCOUNT_NUMBER_LEN=34) and charset is not constrained, so '******7890' (10 chars) passes validation and is persisted as the real account number. The bank account is then permanently corrupted to a masked placeholder, breaking guard payouts, with no error surfaced. The masking makes this corruption silent and the API offers zero protection. The same trap NULLs the account number entirely if the client omits the optional field on a self-edit.
  - Fix: Server-side: reject any write where account_number matches the mask pattern (contains a run of '*'), OR make account_number COALESCE/sticky on upsert (only overwrite when a new, non-masked value is explicitly provided via a dedicated field). Preferably move bank-account edits to a separate, explicit endpoint that never echoes the masked value back into the writable field, and document in OpenAPI that the masked read value must never be re-submitted.
  - Verifier: VERIFIED MECHANICALLY, severity DOWNGRADED high→medium.

The mechanism is exactly as described:
- Read masks: GET /profile/me returns mask_guard_response (api/mod.rs:788-793), turning account_number into e.g. "******7890" via domain/mask.rs:15-24.
- Writes are unconditional: repo/mod.rs:297 (ON CONFLICT DO UPDATE SET account_number = EXCLUDED.account_number) and repo/mod.rs:342 (UPDATE ... account_number = $8). No COALESCE, no "keep existing", no mask-pattern rejection.
- Validation does not sto

- ☐ **[MEDIUM/booking/data-integrity] Utilization heatmap mis-buckets odd scheduled hours (rounds instead of floors), silently dropping hours 22–23**
  - `services/booking/src/repo/mod.rs` 192
  - Why: The utilization query computes the 2-hour bucket as `(EXTRACT(hour FROM scheduled_at) / 2)::int`. In Postgres `EXTRACT(hour ...)` returns numeric, so `23/2 = 11.5` and casting numeric→int ROUNDS (half-to-even), not truncates: hour 23 → bucket 12, hour 19 → 9.5 → 10, hour 21 → 10.5 → 10, etc. The documented/contracted bucket range is 0..11 (see models.rs UtilizationCell and contracts booking.ts `bucket: 0..11`), and the web-admin reports page (`app/(dashboard)/reports/page.tsx:90`) filters `cell.bucket < 12`, so any guard-hours scheduled in the 22:00–23:59 window are mapped to bucket 12 and then SILENTLY DROPPED from the heatmap, and several odd hours land one bucket too high. No test pins the bucket math, so this slipped through. The admin utilization analytic is therefore wrong for any business that schedules into evenings.
  - Fix: Use floor division: `floor(EXTRACT(hour FROM scheduled_at) / 2)::int` (or `(EXTRACT(hour FROM scheduled_at)::int / 2)` to get integer-truncating division). Add a repo test that inserts bookings at hours 21/22/23 and asserts they land in buckets 10/11/11.
  - Verifier: VERIFIED against real Postgres 16. services/booking/src/repo/mod.rs:192 computes the heatmap bucket as `(EXTRACT(hour FROM scheduled_at) / 2)::int`. I empirically confirmed: (a) `EXTRACT(hour ...)` returns type `numeric`; (b) `numeric / 2` retains the .5 fraction; (c) `numeric::int` ROUNDS, not truncates. Tested all 24 hours: current bucket vs floor diverges for every odd hour — hour 1→1(should be 0), 3→2(1), ... 21→11(10), and critically hour 23→bucket 12. The contract is bucket 0..11: models.r

- ☐ **[MEDIUM/booking/concurrency] Outbox relay lacks FOR UPDATE SKIP LOCKED — double-publishes on any scale-out / rolling-deploy overlap**
  - `services/booking/src/repo/mod.rs` 844
  - Why: `fetch_unpublished` selects unpublished rows with a plain SELECT (no row lock) and the relay (`events/mod.rs:run_relay`/`drain_once`) publishes then `mark_published`. With a single replica (current k8s base: replicas:1, no HPA) this is safe, but the booking service has no per-instance leader election or row claim, so the moment it is scaled to >1 replica — OR during a rolling deploy where the old and new pods briefly overlap — both relays fetch the same rows and publish each event twice to NATS. The at-least-once + consumer-dedupe-on-event_id contract keeps this CORRECT (the event_id is stable, stored in the JSONB envelope, so duplicates dedupe), so this is a robustness/scaling hazard rather than a data-loss bug — but the relay is one config change away from doubling every notification/payment-trigger emission until the consumer dedupes, and the code comments claim the relay is the producer half without noting the single-instance assumption.
  - Fix: Make the claim safe under concurrency: `SELECT ... WHERE published_at IS NULL ORDER BY created_at FOR UPDATE SKIP LOCKED LIMIT $1` and mark published inside the same transaction (or add an explicit single-writer/leader guard). At minimum, document the replicas:1 invariant on `run_relay` so a future scale-out doesn't quietly double-emit.
  - Verifier: Verified against the cited code. `repo::fetch_unpublished` (services/booking/src/repo/mod.rs:844-857) selects unpublished outbox rows with a plain `SELECT ... WHERE published_at IS NULL ORDER BY created_at LIMIT $1` — no `FOR UPDATE SKIP LOCKED`, no claim/lease column (the booking.outbox schema, migrations/booking/0001_init.sql:55-61, has only id/topic/payload/created_at/published_at). `drain_once` (services/booking/src/events/mod.rs:102-115) does publish-then-`mark_published` as two separate po

- ☐ **[MEDIUM/calling/concurrency] Busy-callee guard is a check-then-insert race (no DB constraint backs it)**
  - `services/calling/src/repo/mod.rs` 131-159
  - Why: initiate() runs a SELECT for an active call on callee_id inside the tx, then INSERTs if none found. There is no unique index / exclusion constraint enforcing 'one active call per callee' (migration 0001 only has a partial index idx_call_logs_active for read speed, not a UNIQUE). Two concurrent initiate() transactions (e.g. customer and guard dial each other at the same instant, or a double-tap) both run the SELECT, both see no active row (the other's INSERT isn't committed/visible under READ COMMITTED), and both COMMIT — producing two simultaneous 'initiated' calls for the same callee, each emitting a calling.initiated event. The callee then gets two incoming-call pushes and the 'busy' de-dupe the comment promises is defeated under the exact race it's meant to stop. FOR UPDATE is used in apply_transition but NOT in the busy SELECT here, so there is no row lock to serialize the two initiators.
  - Fix: Serialize concurrent initiations against the same callee. Either take a pg_advisory_xact_lock(hashtext(callee_id::text)) at the top of the tx before the busy SELECT, or add a partial UNIQUE index (e.g. CREATE UNIQUE INDEX CONCURRENTLY ... ON calling.call_logs (callee_id) WHERE status IN ('initiated','accepted','connected')) and treat the unique-violation as the 'busy' Conflict. The advisory lock is the least disruptive given the intentional 30s-window semantics.
  - Verifier: Verified against services/calling/src/repo/mod.rs:117-160 and contracts/db/migrations/calling/0001_init.sql:33-57. initiate() runs a busy SELECT (query_scalar, fetch_optional, lines 131-138) on callee_id with active-status + 30s window WITHOUT FOR UPDATE or any lock, then INSERTs a new row (lines 144-155) if none found, then commits. The migration's idx_call_logs_active (lines 55-57) is a PLAIN partial index ON (callee_id) WHERE status IN (...), NOT a UNIQUE/exclusion constraint — confirmed ther

- ☐ **[MEDIUM/presence/concurrency] Stale socket's set_offline clobbers a freshly-reconnected live session (last-disconnect-wins)**
  - `services/presence/src/api/ws.rs` 275-278 (set_offline call); services/presence/src/repo.rs:118-124
  - Why: guard_locations is ONE row per guard (PK guard_id) and set_offline runs an UNCONDITIONAL `UPDATE ... SET is_online = false WHERE guard_id = $1` on ANY session exit. The module doc explicitly states there is no shared session registry, so two concurrent sockets for the same guard_id are possible (a flaky-network guard reconnects → socket A opens and is streaming, then socket B's ping-pong reaper finally fires or B's TCP RST lands → B's teardown flips is_online=false on the row A is actively using). There is no fencing on connection identity or recency, so the later-disconnecting OLD socket overwrites the online flag of the live NEW socket. The guard then disappears from the admin live map and from discovery's is_live filter even though they are connected and streaming. It self-heals only on A's next real GPS fix (heartbeats deliberately do NOT set online), so an idle-but-connected guard can stay falsely offline until their next fix — exactly the kind of false-offline the freshness design is trying to avoid.
  - Fix: Fence the offline write on a per-connection token. Either (a) carry a session/connection UUID into guard_locations (e.g. a connected_session column set on each upsert) and make set_offline a conditional `UPDATE ... SET is_online=false WHERE guard_id=$1 AND connected_session=$2` so only the owning session can mark itself offline; or (b) keep a small in-process registry (DashMap<guard_id, session_id>) and skip set_offline when a newer session has superseded this one. Option (a) is robust across replicas.
  - Verifier: VERIFIED REAL. Every load-bearing claim checks out against the cited code:

1. One row per guard: `guard_locations` PK is `guard_id` — `upsert_location` uses `ON CONFLICT (guard_id) DO UPDATE` (repo.rs:75) and all reads key on `WHERE guard_id = $1`. Socket A and B write the SAME row.

2. Unconditional offline write: `set_offline` is a blanket `UPDATE presence.guard_locations SET is_online = false WHERE guard_id = $1` (repo.rs:118-124) with NO fencing on connection identity. It runs after the ses

- ☐ **[MEDIUM/presence/performance] Admin bulk /locations and per-guard /history read the PRIMARY pool, not the read replica (C5.3 inconsistency)**
  - `services/presence/src/api/mod.rs` 35 (list_locations uses state.db()); 65 (guard_history uses state.db())
  - Why: CLAUDE.md locks in 'read replica for report/list' (C5.3), and peer services follow it: rating (state.rs db_read for review lists + summary) and booking (state.rs db_read for list/discovery) both wire a separate replica pool via shared::db::create_read_pool and route their heavy list reads to it. presence holds only a single `db: PgPool` (state.rs:49) and serves BOTH the high-frequency GPS write path (upsert_location/insert_history on every fix, 1/sec per guard × fleet) AND the read-heavy admin bulk read (up to LOCATIONS_MAX=5000 rows, ORDER BY recorded_at DESC) and history playback (up to 1000 rows) on that same primary pool. The bulk live-map read and history playback are precisely the report/list reads C5.3 directs to the replica; running them on the primary lets a large admin map scan or a history export contend for connections with the GPS ingest writes on the same pool. This is an architecture-consistency + write-path-isolation gap, not a hard correctness bug.
  - Fix: Add a db_read: PgPool to AppState (wired from shared::db::create_read_pool, falling back to primary when DATABASE_READ_URL is unset, as booking/rating do) and add a PresenceDeps::db_read() default-to-primary accessor. Route repo::list_locations and repo::history reads through db_read(); keep upsert/insert/set_offline + the IDOR authz read on the primary.
  - Verifier: Verified against the cited code. presence/src/state.rs holds a single `db: PgPool` (line 49) and PresenceDeps exposes only `db()` — there is no `db_read()`. The two cited reads route through the primary: list_locations uses state.db() (api/mod.rs:35; repo::list_locations bulk-scans up to LOCATIONS_MAX ORDER BY recorded_at DESC) and guard_history uses state.db() (api/mod.rs:65; repo::history with limit/offset). Crucially, the high-frequency GPS write path shares that SAME pool: ws.rs:98 clones st

- ☐ **[MEDIUM/notification/concurrency] Broadcast send is non-atomic: concurrent /send (or /send racing the scheduler) double fans-out to every recipient**
  - `services/notification/src/api/mod.rs` 353-370 (send_broadcast), 399-417 (dispatch_due_broadcasts); repo 291-313 (mark_broadcast_sent)
  - Why: send_broadcast does read-check-act with no atomic claim: get_broadcast() reads status, checks `b.status == "sent"`, then fan_out() inserts a notification_logs row + FCM push per recipient, then mark_broadcast_sent() flips status. mark_broadcast_sent's WHERE clause is `WHERE id = $1` only — it does NOT guard on the prior status. So two concurrent POST /admin/broadcasts/{id}/send (two admin tabs, or a retry) both pass the `!= sent` check before either marks sent, and both run the full fan-out — every recipient gets the in-app notification and FCM push TWICE. The same race exists between POST /send and the 30s scheduler tick: due_broadcasts() selects `status='scheduled'` rows with no claim/SKIP LOCKED, so a manual /send on a scheduled broadcast can interleave with dispatch_due_broadcasts picking the same row. Unlike the event path (process_event uses an atomic INSERT ... ON CONFLICT claim), the broadcast path has no idempotency guard at all.
  - Fix: Make the status flip the atomic claim: change mark_broadcast_sent (and add a 'claim for send' step) to UPDATE ... SET status='sent', recipient_count=$2, sent_at=now() WHERE id=$1 AND status IN ('draft','scheduled') RETURNING ...; only proceed with fan_out when the claiming UPDATE affected a row (rows_affected==1). Equivalently, claim the broadcast to an in-progress/sending status first (UPDATE ... WHERE status IN ('draft','scheduled')), and have due_broadcasts use FOR UPDATE SKIP LOCKED so the scheduler and /send can't both grab it.
  - Verifier: Verified against the cited code. send_broadcast (services/notification/src/api/mod.rs:353-370) does read-check-act: get_broadcast reads status and checks `b.status == "sent"` (line 362), then runs fan_out (line 365), then mark_broadcast_sent (line 366) — no atomic claim spans the check and the side effect. mark_broadcast_sent (services/notification/src/repo/mod.rs:291-313) flips status with `WHERE id = $1` only (line 303), no prior-status guard. fan_out (api/mod.rs:204-239) inserts a notificatio

- ☐ **[MEDIUM/notification/security] FCM device tokens are never pruned and not made unique per device, so a reassigned/stale token delivers one user's notifications to another user's device**
  - `services/notification/src/repo/mod.rs` 22-42 (register_token), 53-60 (user_tokens); fcm.rs 270-279 (send_one swallows errors)
  - Why: fcm_tokens has UNIQUE(user_id, token) — the SAME physical-device FCM token can be stored under multiple user_ids. FCM reassigns a registration token to a new app instance/user, and the platform tells you via an UNREGISTERED / registration-token-not-registered error on send. send_one() (fcm.rs) only logs `FCM API error` and drops it on the floor; nothing ever DELETEs the stale row. Combined with no logout-side cleanup guarantee, this means: user A logs in on a device (token T stored for A), later user B uses that device and FCM rotates/reassigns T to B — A's row for T persists, so when A receives a booking/payment notification, deliver()/user_tokens() pushes A's (potentially sensitive: payment received, booking details) push to the device now held by B. This is a real cross-user information-disclosure path, and it also wastes FCM quota pushing to dead tokens forever.
  - Fix: On send, inspect the FCM v1 error: when the body is UNREGISTERED / NOT_FOUND / INVALID_ARGUMENT for the token, DELETE FROM notification.fcm_tokens WHERE token = $1 (across all users). Additionally consider a UNIQUE(token) constraint (a device token belongs to at most one user at a time) so register_token re-points the token to the current user instead of leaving a duplicate row, and ensure logout calls unregister_token.
  - Verifier: The code-level facts are accurate. Verified in-repo: `notification.fcm_tokens` has `UNIQUE (user_id, token)` (contracts/db/migrations/notification/0001_init.sql:31), so the same device token can be stored under multiple user_ids. `send_one()` (services/notification/src/fcm.rs:270-279) only `tracing::warn!`s FCM errors and never inspects the body for UNREGISTERED/NOT_FOUND nor deletes any row; nothing anywhere prunes tokens. Both push paths — `deliver()` (api/mod.rs:38-56) and `process()` (events

- ☐ **[MEDIUM/chat/data-integrity] No uniqueness on conversations.request_id — duplicate conversations per booking accumulate and split a thread**
  - `contracts/db/migrations/chat/0001_init.sql` 30-38 (plain index, no UNIQUE) + repo/mod.rs:106-113
  - Why: chat.conversations has only a plain `idx_chat_conversations_request` on request_id, no UNIQUE. create_conversation always INSERTs a new row (repo/mod.rs:106). The mobile client even documents this: chat_launcher.dart 'POST /conversations is NOT idempotent ... two TRULY-concurrent callers ... could both miss the find and both create ... a server-side unique(request_id) ... would close it fully.' Effects: (1) a benign race (two entry points) or a malicious client (finding #1) creates N conversation rows for one booking; the enriched list then shows duplicate threads, messages scatter across rows, and unread/last-message become inconsistent. (2) internal_set_request_status updates ALL rows for a request_id (repo:484), which masks the dup at status-push time but not for messaging. The repo's participant upsert is ON CONFLICT-guarded, but the conversation row itself is not.
  - Fix: Add `CREATE UNIQUE INDEX CONCURRENTLY ... ON chat.conversations (request_id)` (1:1 booking↔conversation per the schema's own '1:1 booking-scoped messaging' header), and make create_conversation upsert/return-existing on conflict (or return 409 with the existing id) so the client's find-then-create collapses to an idempotent get-or-create. This also hardens finding #1 by preventing fabricated parallel rows for a real booking.
  - Verifier: Verified against the cited code. chat.conversations has only a plain index idx_chat_conversations_request on request_id (0001_init.sql:38), NOT UNIQUE, despite the schema header's own "1:1 booking-scoped messaging" claim (line 3). repo::create_conversation (repo/mod.rs:106-113) always does a bare INSERT with no ON CONFLICT and no return-existing, and the API handler create_conversation (api/mod.rs:63-75) only enforces a participant-membership gate — it performs NO find-then-create and NO dedup s

- ☐ **[MEDIUM/contracts+packages/api-contract] PDPA data-export internal endpoint (/internal/users/{user_id}/export) is registered in 4 services but absent from every OpenAPI spec**
  - `contracts/openapi/booking.yaml (+ rating.yaml, payment.yaml, profile.yaml)` booking router services/booking/src/main.rs:217; profile services/profile/src/main.rs:197; rating services/rating/src/main.rs:113; payment services/payment/src/main.rs:114
  - Why: contracts/README.md declares the OpenAPI specs 'the source of truth' and explicitly documents that /internal/* endpoints must carry serviceAuth (the v2 fix for v1's unauthenticated /internal/push). The booking and rating specs DO document their other internal endpoints (/internal/bookings/{id}, /internal/guards/{id}/rating-summary), but the live, registered route GET /internal/users/{user_id}/export — the PDPA §19/§32 data-export aggregation surface that identity's ExportClient fans out to (services/identity/src/export_client.rs:168) — is in NONE of booking.yaml/rating.yaml/payment.yaml/profile.yaml. payment.yaml has no /internal block at all despite serving this route. This breaks R4 (OpenAPI<->handler parity): the source of truth understates the real attack surface, and any contract-driven client/pact/security-review/codegen run will be blind to a privacy-sensitive endpoint that returns a user's full PII export. Auth is fine (every handler takes ServiceCaller — verified), so this is a documentation-parity gap, not an exploit.
  - Fix: Add the GET /internal/users/{user_id}/export operation (operationId internalExportUser, security: [serviceAuth], param user_id uuid, 200 ApiResponse<object>, 401/403) to booking.yaml, rating.yaml, payment.yaml, and profile.yaml — mirroring the already-documented internal ops. Add a CI parity check (handler-route list vs spec paths) so a registered route without a spec entry fails the build.
  - Verifier: Verified against the actual code. The route GET /internal/users/{user_id}/export is registered in all 4 services (services/booking/src/main.rs:217, services/profile/src/main.rs:198, services/rating/src/main.rs:114, services/payment/src/main.rs:115), each mapped to api::internal_export_user. Grepping contracts/openapi/{booking,rating,payment,profile}.yaml for /export, internalExportUser, or users/.*export returns ZERO matches — the operation is in none of the four specs. payment.yaml has no /inte

## 🟡 LOW + nit (39)

- ☐ **[LOW/api-gateway/security] Edge rate-limit IP key trusts left-most X-Forwarded-For with no in-code trusted-hop guard**
  - `services/api-gateway/src/ratelimit.rs` 25-33
  - Why: client_ip() takes the left-most X-Forwarded-For entry unconditionally. XFF is fully client-controllable, so if it is ever the client value, an attacker rotates the left-most token per request to mint a fresh fixed-window bucket each time and bypass every per-IP limit (the auth brute-force and OTP SMS-cost guards — v1 risks #4/#5). This is mitigated TODAY by deployment: nginx.staging.conf sets `proxy_set_header X-Forwarded-For $remote_addr` (overwrite, not append) and ingress.yaml documents keeping `use-forwarded-headers: false`, so the trusted hop overwrites XFF. The risk is that the safety lives entirely in ops config the gateway cannot see — a single ingress misconfiguration (e.g. enabling forwarded-headers without a proxy-real-ip-cidr, or fronting the gateway with an L7 LB that appends) silently turns the limiter into a no-op with no code-level signal. The gateway has the real TCP peer in ConnectInfo and could enforce a configured trusted-proxy/hop-count instead of blindly trusting the header.
  - Fix: Make XFF trust explicit in code: gate it behind a `TRUSTED_PROXY_HOPS`/`TRUST_XFF` env (default off → use socket peer), and when enabled take the Nth-from-right hop rather than the left-most. At minimum, add a startup warning if XFF parsing is enabled without a configured trusted-proxy bound.
  - Verifier: minor — reported unverified

- ☐ **[LOW/api-gateway/security] Connection header listed tokens are not honored when stripping hop-by-hop headers**
  - `services/api-gateway/src/domain/headers.rs` 19-39
  - Why: is_hop_by_hop() matches only a fixed allowlist of header NAMES. RFC 7230 §6.1 also makes any header *named in the inbound `Connection` header* hop-by-hop (e.g. `Connection: x-foo` means strip `x-foo`). The proxy forwards the inbound Connection header tokens are not parsed, so a client could mark an arbitrary header connection-scoped and the gateway would still forward it to the upstream. Impact here is limited because identity (x-user-*) is force-stripped and re-injected and the literal `connection` header itself IS dropped, so this cannot smuggle identity; the residual risk is forwarding a header an upstream treats as connection-scoped, enabling minor desync/cache confusion behind certain upstreams. Worth closing for a security-boundary proxy.
  - Fix: Parse the inbound `Connection` header, split on commas, and add each listed token (lowercased, trimmed) to the per-request drop set before copying headers in build_forward_headers().
  - Verifier: minor — reported unverified

- ☐ **[LOW/api-gateway/security] Edge strips only W3C traceparent/tracestate, not other client-supplied trace-context headers**
  - `services/api-gateway/src/proxy.rs` 71-73
  - Why: build_forward_headers() removes `traceparent`/`tracestate` so a client cannot forge the downstream W3C trace context, then re-injects the gateway's own via observability::inject_context(). But B3 (`x-b3-traceid`, `x-b3-spanid`, `x-b3-sampled`, `b3`) and `baggage` are not stripped. If any upstream's tracer reads B3 or baggage, a client could still force/forge a downstream trace_id, sampling, or inject attacker-controlled baggage entries that ride into logs/spans. The wsproxy path uses a strict allowlist (only Authorization + minimized cookie cross), which is the safer model; the REST proxy uses a denylist that this gap slips through.
  - Fix: Also drop `b3`, `x-b3-*`, and `baggage` in build_forward_headers (or, better, switch to an allowlist of forwardable client headers as the WS proxy already does).
  - Verifier: minor — reported unverified

- ☐ **[LOW/api-gateway/api-contract] Booking-status WS snapshot returns a 404 'Booking not found' that distinguishes from the 403 non-participant response**
  - `services/api-gateway/src/ws.rs` 173-187
  - Why: fetch_snapshot() maps booking's 403 to a generic 'Not a participant' and its 404 to 'Booking not found'. The 200/snapshot path and the comment at line 176-177 explicitly aim for 'no existence oracle', but returning a distinct 404 vs 403 lets a caller probe which booking UUIDs exist (a real booking they are not a participant in → 403; a non-existent id → 404). The upstream booking service produces this distinction, so the oracle ultimately depends on booking's own behavior, but the gateway re-surfaces it on the WS handshake. Low because booking ids are UUIDs (not enumerable) and this only reveals existence, not content.
  - Fix: Collapse 403 and 404 to the same generic forbidden response on the WS upgrade (return 403 for both), matching the no-existence-oracle intent stated in the surrounding comments.
  - Verifier: minor — reported unverified

- ☐ **[LOW/api-gateway/performance] Edge auth issues two sequential Redis round-trips (jti EXISTS then trv GET) per protected request**
  - `services/api-gateway/src/auth.rs` 98-114
  - Why: validate() performs `EXISTS revoked_jti:{jti}` and `GET user_trv:{sub}` as two awaited, sequential commands on every protected request — and the backend then repeats both via its own AuthUser/authenticate_token (defense-in-depth). That is up to 2 edge + 2 backend Redis round-trips per request on the hot path. Functionally correct and fail-closed, but it doubles per-request Redis latency versus a pipelined MGET/pipeline. At target throughput (api tier 30 r/s/IP across many IPs) the added latency and Redis op count are non-trivial.
  - Fix: Pipeline the two lookups (redis::pipe().exists(jti).get(trv)) into a single round-trip; this halves the edge Redis RTT without changing semantics.
  - Verifier: minor — reported unverified

- ☐ **[LOW/identity/correctness] Erased/deactivated user's legitimate refresh attempt is misclassified and logged as 'token reuse detected'**
  - `services/identity/src/domain/rotation.rs` 55-66
  - Why: soft_delete_and_redact and revoke_all set every refresh row to revoked=TRUE. When such a (now-erased or force-revoked) user later presents a refresh token they still hold, find_refresh_by_rotation returns the row, the secret verifies, and decide() sees revoked=true and returns ReuseDetected (rotation.rs:56-57), which drives the refresh handler to call revoke_family again and emit a tracing::warn! 'refresh-token reuse detected — revoking family' (api/mod.rs:216-225). This conflates a benign post-revocation/post-erasure refresh with a genuine RFC 6749 §6 reuse attack, polluting the security signal that future alerting will key off of. No incorrect access is granted (the request is still rejected with a generic 401), so this is low severity — but it will generate false-positive reuse alarms for every erased/revoked user who refreshes.
  - Fix: Distinguish 'revoked because the whole family/account was deliberately killed' from 'revoked because this specific token was already rotated' before classifying as reuse — e.g. carry a family-revocation/account-deactivation reason (or check is_active/deleted_at) so an account-level revoke yields a benign 401 rather than a reuse alert, keeping the reuse signal clean for true replay attacks.
  - Verifier: minor — reported unverified

- ☐ **[LOW/otp/security] /otp/verify returns three distinguishable failure messages, contradicting its own 'same error for wrong/expired/exhausted' contract**
  - `services/otp/src/api/mod.rs` 192 / 205 / 212
  - Why: The api/mod.rs module header (lines 5-7) states verify 'returns the same error for wrong/expired/exhausted codes'. The implementation returns three different bodies, all HTTP 400: 'OTP ไม่ถูกต้องหรือหมดอายุ' for wrong-length or no-live-code (lines 192, 198), 'เกินจำนวนครั้งที่อนุญาต กรุณาขอ OTP ใหม่' for attempts-exceeded (line 205), and 'OTP ไม่ถูกต้อง' for a wrong-but-valid-length code against a live row (line 212). This lets a caller distinguish 'a live code exists and you typed it wrong' from 'no live/expired code' from 'attempts exhausted', acting as a small per-phone state oracle that aids a brute-forcer in tracking their own progress and confirms whether an unconsumed code is currently outstanding. It is not user-enumeration (it concerns the caller's own phone), but it breaks the documented generic-response guarantee the checklist (§9) calls for.
  - Fix: Collapse all verify failure paths to one identical generic message + status (e.g. 'OTP ไม่ถูกต้องหรือหมดอายุ'), as the docstring promises. The attempts-exceeded burn can still happen server-side; just return the same body the wrong-code path returns. Update the header comment to match whatever is chosen.
  - Verifier: minor — reported unverified

- ☐ **[LOW/otp/security] OTP request will SMS any syntactically-valid Thai number — residual third-party SMS-bombing surface**
  - `services/otp/src/api/mod.rs` 63-176
  - Why: request validates only phone *format* (domain::validate_thai_phone, 10 digits starting with 0) and never checks that the number is registered/known before dispatching an SMS (api/mod.rs:92, 169). This is intentional for the registration flow (the phone need not pre-exist), but it means the endpoint can be used to deliver attacker-chosen text-free OTP SMS to arbitrary third-party numbers. Throttling exists (math-captcha GETDEL, per-phone 60s cooldown, BURST_THRESHOLD=3 / daily cap 10, plus the gateway's per-IP Tier::Otp bucket), so volume per target is bounded, but the captcha is a trivial single-operand addition (operands 1-19, answer ≤ 38) and is easily solved programmatically, leaving per-IP + per-phone caps as the only real barrier to spraying many distinct victim numbers from rotating IPs.
  - Fix: Accept as residual risk if product requires open registration, but consider: (a) a global per-IP and per-/24 daily send cap in addition to the per-phone cap, (b) strengthening the captcha (multi-term or non-addition) since the current one is near-trivial for bots, and/or (c) monitoring/alerting on high distinct-recipient fan-out from a single source. No code-correctness change required.
  - Verifier: minor — reported unverified

- ☐ **[LOW/profile/error-handling] Document upload key persists but a re-upload orphans the previously-stored S3 object**
  - `/Users/nest/Documents/pguard/services/profile/src/api/mod.rs` upload_guard_document 517-525; upload_guard_avatar 671-680
  - Why: Each upload generates a fresh UUID key (profile/{user_id}/documents/{uuid}.{ext}) and overwrites the *_key column with the new key. When a guard (or admin) RE-uploads the same document_type, the previous object's key is overwritten in the DB but the OLD S3 object is never deleted — it orphans permanently. Unlike the DB-failure compensation path (delete_best_effort on write failure), there is no read-old-key-then-delete-after-successful-overwrite step. Over time this accumulates unbounded orphaned PII objects (ID-card scans) in the private bucket, which is both a storage-cost and a PDPA data-minimization/erasure concern (DELETE /me erasure won't find keys that are no longer referenced by any column). The current code only avoids orphaning on the failure path, not on the happy re-upload path.
  - Fix: Before overwriting the column, SELECT the existing key for (user_id, column); after the new key is committed, call s3().delete_best_effort(old_key) for the superseded object. Same for avatar_key. Track residual orphans via a sweep job if a best-effort delete fails.
  - Verifier: Verified true against the cited code. In `upload_guard_document` (services/profile/src/api/mod.rs:514-525) and `upload_guard_avatar` (665-680), each upload generates a fresh UUID key (`profile/{user_id}/documents/{uuid}.{ext}` / `.../avatar/{uuid}.{ext}`), uploads to S3, then calls `repo::update_document_key`. That repo helper (src/repo/mod.rs:416-435) is a blind `UPDATE ... SET {column} = $2` — it does not read or return the prior key, and the handlers never call `get_document_key` before overw

- ☐ **[LOW/profile/data-integrity] document_expiry rows are never cleaned up on guard rejection/erasure and have no FK/owner check at write**
  - `/Users/nest/Documents/pguard/services/profile/src/api/mod.rs` set_guard_document_expiry 597-634; repo upsert_document_expiry 129-148
  - Why: set_guard_document_expiry (own-or-admin) and apply_document_expiries upsert into profile.document_expiry keyed on (guard_id, document_type) with NO check that a guard_profiles row exists for guard_id (the table has guard_id as a bare UUID, no FK — by design — but also no application-level existence guard, unlike update_document_key which 404s on a missing profile). An admin PUT for a random/typo user_id silently creates an orphan expiry row that then surfaces in GET /admin/documents/expiring forever. It is low severity because document_type is allowlisted and the array is capped, but it lets the admin 'expiring documents' surface be polluted with rows for non-existent guards and the rows are never tombstoned on reject/erasure.
  - Fix: Gate upsert_document_expiry on the existence of a profile.guard_profiles row for guard_id (return 404 like update_document_key), and delete document_expiry rows for a guard on rejection/erasure so the admin surface stays accurate.
  - Verifier: minor — reported unverified

- ☐ **[LOW/profile/security] internal_export_user returns the full unmasked account number with no scoping assertion beyond the path id**
  - `/Users/nest/Documents/pguard/services/profile/src/api/mod.rs` internal_export_user 918-925; repo export_user_data 859-958
  - Why: GET /internal/users/{user_id}/export is ServiceCaller-gated (gateway blocks /internal/) and returns the FULL account_number plus all PII for the path user_id. This is correct for a data-subject self-export proxied by identity's aggregator, but the handler trusts that the calling service has already authorized that user_id belongs to the requesting data subject — there is no constraint tying the service-JWT caller to the user_id, so ANY service holding a valid service-JWT (or a compromised internal caller) can dump any user's full bank PII by iterating user_ids. Least-privilege would scope this read. Low because /internal/ is not edge-reachable and service-JWT is a separate secret, but the unmasked full-PII fan-out from a single service credential is a meaningful blast-radius concern worth noting.
  - Fix: Either accept and verify a per-subject assertion (e.g. a signed export grant carrying the authorized user_id) from identity, or restrict the caller set (sub must equal 'identity-service') so only the data-export aggregator can reach it, and log/audit each export of full bank PII.
  - Verifier: minor — reported unverified

- ☐ **[LOW/payment/data-integrity] Post-pay charge never persists actual_hours / final_amount — proration breakdown is silently discarded**
  - `services/payment/src/events/consumer.rs` 216-243 (finalize) and repo charge_idempotent 228-243
  - Why: The consumer computes the bill via domain::post_pay_charge(base_fee, booked_hours, guard_count, tip, actual_seconds), which internally prorates the base to the worked hours. But finalize() throws away every intermediate: it passes only `amount` (== expected_total) into charge_idempotent, and the INSERT writes only booking_id/customer_id/guard_id/amount/expected_total/payment_method/status/paid_at. The columns actual_hours and final_amount (NUMERIC, created exactly for this in migration 0001/0002) are NEVER written, so every post-pay payment row has actual_hours=NULL and final_amount=NULL. Consequence: the customer-facing GET /payments and the PDPA data export can show the total charged but cannot show how many hours were actually billed or the base-vs-tip split — there is no receipt detail. It also makes the NET_REVENUE_EXPR's `COALESCE(final_amount, amount)` always fall through to `amount` (harmless today, but the column it was designed around is dead). For 'payments + receipts' (main.rs module doc) this is a transparency/auditability gap on the money path: a disputed bill cannot be reconstructed from the payment row alone.
  - Fix: Have post_pay_charge (or a sibling) return the proration breakdown (actual_hours clamped, prorated base) alongside the total, and persist actual_hours + final_amount in charge_idempotent so each completed payment row is a self-contained receipt. At minimum store actual_hours from the event's actual_seconds.
  - Verifier: Verified against the cited code. In services/payment/src/events/consumer.rs finalize() (lines 220-240), the consumer calls domain::post_pay_charge(base_fee, booked_hours, guard_count, tip, actual_seconds), which returns ONLY the total Decimal; the per-completion proration breakdown (clamped actual_hours, prorated base) computed internally by compute_proration in domain/proration.rs is discarded. It then calls repo::charge_idempotent with (amount, amount, ...). In repo/mod.rs charge_idempotent (l

- ☐ **[LOW/payment/observability] Old-shape booking.completed events are ACKed and dropped → silent un-billed completion during a deploy window**
  - `services/payment/src/events/consumer.rs` 150-164
  - Why: base_fee/guard_count/booked_hours/tip are required (no serde default) in CompletedPayload. A booking.completed emitted by a pre-deploy booking instance (or any future schema change that drops a field) fails serde_json::from_slice, is treated as poison, ACKed (dropped) and counted via record_rejected_event. That is the right call for genuinely un-fixable poison, but here it means a real completed job is permanently never billed — the event is gone from the consumer's view and there is no dead-letter or reconciliation hook, only a Prometheus counter and a log line. Revenue loss is silent unless an operator notices the counter and manually reconciles against booking. The code comments acknowledge this but the mitigation is only 'deploy payment before booking' discipline.
  - Fix: Route parse failures to a dead-letter subject/table (or NAK with a bounded redelivery + max-deliver to a DLQ) instead of an immediate ACK-and-forget, so an un-billable completion is recoverable. Alert on record_rejected_event(DURABLE) > 0 rather than relying on a human reading the metric.
  - Verifier: VERIFIED the code path. consumer.rs:56-67 makes base_fee/guard_count/booked_hours/tip REQUIRED (only guard_id/actual_seconds have #[serde(default)]). consumer.rs:155-164: a parse failure (e.g. a missing required field from an old-shape event) calls record_rejected_event(DURABLE), logs error, and ACKs (drops) the message — no DLQ, no dead-letter table, no reconciliation hook. The comment (150-154) explicitly acknowledges a real completed job lands here and is "dropped, never billed." So the core 

- ☐ **[LOW/payment/architecture] payment.processed_events idempotency ledger is created but never read or written (dead schema vs. code drift)**
  - `contracts/db/migrations/payment/0002_expected_total_and_consumer.sql` 26-31
  - Why: Migration 0002 creates payment.processed_events and documents it as 'the idempotency ledger for the booking.completed consumer ... dedupe on event_id ... claim the id + finalize proration in ONE transaction'. The consumer and repo never touch this table (grep across services/payment returns nothing). Idempotency is instead enforced entirely by the uq_payment_one_completed_per_booking unique partial index + ON CONFLICT DO NOTHING — which is actually a stronger guarantee for double-charge (one charge per booking regardless of how many distinct completion events arrive), so there is no correctness bug. But the migration comment is now misleading: a future maintainer reading it will believe event_id dedup is the mechanism and may 'fix' the missing claim, or build refund logic on top of the unused ledger. The migration also still says refund_status 'drives the admin refund workflow' and 0002 header says 'The charge handler rejects any amount below it' — both describe a charge-handler/refund flow that no longer exists under post-pay.
  - Fix: Either drop payment.processed_events (and the stale refund/charge-handler comments) since booking_id-level idempotency is the real mechanism, or wire the consumer to claim event_id in the same tx if event-level dedup is genuinely wanted. Update migration 0002's header to describe the actual post-pay flow.
  - Verifier: minor — reported unverified

- ☐ **[LOW/payment/data-integrity] Stale expected_total semantics: doc says full booked total, code stores the prorated bill**
  - `services/payment/src/models.rs` 42
  - Why: PaymentResponse.expected_total is documented as 'Server-computed authoritative total at charge time (base_fee × hours × guards + tip)' (the FULL booked total), and migration 0002 calls it 'the SERVER-computed authoritative total ... The charge handler rejects any amount below it'. Under post-pay the consumer sets expected_total = amount = post_pay_charge (the PRORATED base + tip), so expected_total is NOT the full booked total and there is no longer any 'reject amount below expected_total' check (no charge handler exists). The two fields are always equal by construction. This is doc/intent drift, not a money error (amount itself is correct), but it makes expected_total useless as an over/under-charge audit signal — it can never reveal that the customer was billed less than the booked total, which is the whole point of a 'will-not-undercut' column.
  - Fix: Decide expected_total's meaning: if it should remain the full booked total (so a prorated amount < expected_total is visible/auditable), set it to pricing::expected_total(base_fee, booked_hours, guard_count, tip) instead of the prorated amount; then update the docstrings. Otherwise drop the column to avoid implying an audit guarantee that isn't there.
  - Verifier: minor — reported unverified

- ☐ **[LOW/payment/correctness] Revenue report subtracts refund_amount across all statuses while only counting completed rows as positive revenue (dormant double-subtract)**
  - `services/payment/src/repo/mod.rs` 117-119
  - Why: NET_REVENUE_EXPR = SUM(CASE WHEN status='completed' THEN COALESCE(final_amount, amount) ELSE 0 END) - SUM(COALESCE(refund_amount,0)). The positive side only counts status='completed' rows, but the refund subtraction runs over ALL rows regardless of status. If a row is ever moved to status='refunded' with refund_amount set, its positive contribution drops to 0 (ELSE branch) yet its refund is still subtracted, double-penalizing net revenue and potentially producing negative daily revenue. This is currently dormant because the post-pay consumer never sets refund_amount or status='refunded' (there is no refund flow in v2), but the report code is written as if refunds exist, so the first time refunds are wired the analytics will be wrong.
  - Fix: When refunds are introduced, compute net per-row consistently (e.g. SUM over completed rows of COALESCE(final_amount, amount) - COALESCE(refund_amount,0)) rather than subtracting refunds from a separate all-status SUM. Until then, add a test asserting current behavior so the gap is documented.
  - Verifier: minor — reported unverified

- ☐ **[LOW/payment/data-integrity] No DB CHECK that money columns are non-negative (defense-in-depth on the money path)**
  - `contracts/db/migrations/payment/0001_init.sql` 36-41
  - Why: amount/final_amount/refund_amount/expected_total are NUMERIC(12,2) with no CHECK (>= 0). Today the only writer is charge_idempotent fed by domain::post_pay_charge, which is provably non-negative (compute_proration clamps to [0, booked] and tip uses .max(0)), and base_fee is validated >= 0 in booking — so a negative amount cannot currently be inserted. But the schema is the last line of defense on the money path and a future writer/migration could bypass the domain function; a negative amount would silently corrupt revenue analytics. A CHECK is cheap insurance given the table is empty.
  - Fix: Add CHECK (amount >= 0), and CHECK (final_amount IS NULL OR final_amount >= 0), CHECK (refund_amount IS NULL OR refund_amount >= 0) to payment.payments.
  - Verifier: minor — reported unverified

- ☐ **[LOW/booking/observability] Stale migration comment claims work_started_at is set on en_route, but it is only stamped by the explicit /start action**
  - `contracts/db/migrations/booking/0002_pricing_and_worktiming.sql` 22
  - Why: Migration 0002 and the `complete_booking` lineage comments say `work_started_at` is 'set on en_route', but the actual code path stamps it only in `repo::start_job` (the PUT /bookings/{id}/start action) while status is `arrived` — en_route never touches it. The behavior is internally consistent and correct (proration uses the start stamp), but the contradicting comment is a real maintenance trap: a future reader prorating from 'en_route time' would compute the wrong worked duration. Pure documentation drift, no runtime impact.
  - Fix: Update the 0002 migration comment (and any en_route references) to state work_started_at is stamped by the guard's explicit start action while `arrived`, which is the proration basis.
  - Verifier: minor — reported unverified

- ☐ **[LOW/booking/api-contract] admin_assign_guard does not validate the target guard_id is a real approved guard**
  - `services/booking/src/api/mod.rs` 446
  - Why: POST /admin/bookings/{id}/assign binds whatever `guard_id` the admin sends straight into the booking via the ClaimUnassigned transition, with no check that the UUID is an actual approved guard (acknowledged in the handler doc). An admin typo or a stale web-admin list entry assigns the booking to a non-existent/non-guard user; downstream the job_accepted event routes a notification to a phantom user and the booking is stuck assigned to a UUID no guard owns (no guard can then drive its lifecycle, since the assigned-guard gate keys on that id). This is admin-only (low blast radius / trusted actor), so it is low rather than high, but it is a real integrity gap.
  - Fix: Add a service-JWT'd profile point-lookup (`/internal/guards/{id}`) and reject the assign with 400/404 if the target is not an approved guard — the handler doc already flags this as the intended follow-up.
  - Verifier: minor — reported unverified

- ☐ **[LOW/rating/security] submit_review leaks booking existence (404 vs 403) — contradicts the stated generic-403 goal**
  - `services/rating/src/api/mod.rs` 63-70
  - Why: In submit_review, state.booking_reader().get_booking(assignment_id) is awaited BEFORE the ownership check. When the booking does not exist, HttpBookingReader::get_booking (booking_client.rs:87-89) maps booking's upstream 404 to AppError::NotFound → HTTP 404. When the booking DOES exist but belongs to another customer, the handler returns AppError::Forbidden → HTTP 403 (api/mod.rs:65-70). The comment at line 66 explicitly states the intent: 'Generic 403 — never reveal whether the booking exists / belongs to someone else.' That goal is defeated by the preceding line: an authenticated customer probing assignment IDs can distinguish 'booking exists but not yours' (403) from 'booking does not exist' (404), i.e. a booking-existence oracle. Real exploitability is bounded because assignment_id is a UUIDv4 (high entropy, not enumerable by counting), so this is low severity rather than a serious IDOR — but the code does not deliver the security property it claims.
  - Fix: Normalize the not-found and not-owned cases to the SAME response. Easiest: when get_booking returns NotFound, OR when booking.customer_id != user.user_id, return the identical AppError::Forbidden('You can only review your own bookings'). That makes 'no such booking' and 'someone else's booking' indistinguishable, matching the stated comment.
  - Verifier: minor — reported unverified

- ☐ **[LOW/rating/api-contract] Implemented /internal/users/{user_id}/export route is absent from the rating OpenAPI contract**
  - `services/rating/src/main.rs` 112-115
  - Why: main.rs wires GET /internal/users/{user_id}/export (handler internal_export_user in api/mod.rs:197) and identity's export aggregator actually calls it (services/identity/src/export_client.rs:101). CLAUDE.md designates contracts/openapi/<svc>.yaml as the single source of truth, and the other internal endpoint (/internal/guards/{id}/rating-summary) IS documented in rating.yaml. The export endpoint is not present anywhere in contracts/openapi/rating.yaml (grep returns nothing). This is contract drift: the PDPA data-export surface — which returns a user's authored reviews including admin-hidden ones (repo::export_user_reviews) — is undocumented, so codegen/consumers and any contract test have no source-of-truth definition of its shape or its serviceAuth requirement.
  - Fix: Add the GET /internal/users/{user_id}/export operation to contracts/openapi/rating.yaml under security [{ serviceAuth: [] }], documenting the response shape (array of the user's authored reviews) and the 401 response, mirroring how /internal/guards/{id}/rating-summary is already documented.
  - Verifier: minor — reported unverified

- ☐ **[LOW/calling/performance] WS relay registry uses an unbounded channel — a fast peer can balloon a slow peer's queue**
  - `services/calling/src/api/ws.rs` 91-92, 215-219
  - Why: Each session registers an mpsc::unbounded_channel::<String>() sender in the registry, and handle_signal forwards every inbound signal to the peer via send_to (tx.send). If peer B's socket sink drains slower than peer A pushes signals (A floods ICE candidates / oversized SDP, or B's network is congested), the unbounded channel grows without limit in the calling process's heap — there is no backpressure and no per-session/per-second cap inside calling. The gateway proxy caps per-FRAME size (1 MiB) and total concurrent sessions, and presence is noted to cap 1/s, but calling itself enforces neither a rate nor a queue bound, and the doc comment in wsproxy.rs explicitly defers message-rate policy to 'the owning service (...chat/calling own theirs)'. So this is calling's responsibility and it is currently unbounded.
  - Fix: Use a bounded channel (mpsc::channel(N), e.g. 64) and on a full queue either drop the oldest signal or close the slow session, OR add a simple token-bucket rate limit per session in handle_signal (signaling is low-rate by nature). A bounded sender also makes 'peer is offline / can't keep up' observable instead of a silent memory climb.
  - Verifier: Verified against the cited code. services/calling/src/state.rs:28 defines `Registry = Arc<Mutex<HashMap<Uuid, mpsc::UnboundedSender<String>>>>`, and ws.rs:91 creates `mpsc::unbounded_channel::<String>()` per session, registered at ws.rs:92. handle_signal (ws.rs:150-212) forwards every inbound signal to the peer via send_to → tx.send (ws.rs:218) with no rate limit and no queue bound. grep over services/calling/src confirms ZERO rate/bucket/semaphore/bounded logic in the calling path. The gateway 

- ☐ **[LOW/calling/security] calling's own /ws/call does not set max_message_size/max_frame_size (only the gateway does)**
  - `services/calling/src/api/ws.rs` 47-60
  - Why: ws_call() upgrades via ws.on_upgrade(...) without calling .max_message_size()/.max_frame_size(). The 1 MiB client-frame cap exists only at the gateway proxy (wsproxy.rs MAX_CLIENT_FRAME_BYTES). The service binds 0.0.0.0:3008; any client able to reach the pod directly (cluster-internal, a misrouted ingress, or a future east-west path) can send arbitrarily large frames that calling will buffer and then attempt to serde_json::from_str. This is defense-in-depth that every other owning WS service should hold independently of the edge, consistent with the project's 'backends keep their own auth + limits' stance.
  - Fix: Mirror the edge: in ws_call set ws.max_message_size(1<<20).max_frame_size(1<<20).on_upgrade(...). Signaling frames are small JSON, so a tight cap is safe and closes the direct-reach gap.
  - Verifier: minor — reported unverified

- ☐ **[LOW/calling/correctness] WS re-auth closes the live call socket on a transient Redis blip (fail-closed mid-call)**
  - `services/calling/src/api/ws.rs` 120-127
  - Why: The 60s re-auth tick calls authenticate_token(...).is_err() and, on any error, closes the session. authenticate_token fails CLOSED on a Redis error (returns AppError::Internal 'revocation check unavailable'), which is correct for the gate semantics — but here a momentary Redis hiccup during the tick will tear down an in-progress voice/video call even though the token is still perfectly valid and unrevoked. Because the code only inspects is_err() it cannot distinguish 'token revoked/expired' (must close) from 'Redis briefly unreachable' (a connection-manager blip that the reconnecting manager will heal in <1s). The user-visible effect is dropped calls during Redis restarts/failovers, while the security benefit of closing in that exact 1s window is marginal given the next tick (or the call's own end) would catch a genuine revoke.
  - Fix: Match on the error: on AppError::Unauthorized (expired/revoked) close the session; on AppError::Internal (lookup unavailable) keep the socket open and retry on the next tick (optionally after a short grace / N consecutive failures). This preserves force-revoke-all responsiveness while not dropping live calls on a transient cache outage.
  - Verifier: minor — reported unverified

- ☐ **[LOW/calling/api-contract] 'connected' media milestone is silently lost if the answered call already ended (no caller feedback)**
  - `services/calling/src/repo/mod.rs` 280-294
  - Why: CallAction::Connected resolves target=Connected unconditionally, then relies on can_transition(current, Connected) which is only legal from Accepted. If the call is already Ended/Rejected/Missed (terminal) — e.g. the other party hung up in the brief window between media-up and the PUT — the handler returns Conflict('illegal call transition X -> connected'). That is acceptable, but note end_target-style idempotency is NOT applied for connected: a connected POST on an already-connected call returns Conflict('connected -> connected' illegal) rather than a 200 idempotent no-op, so a client that retries the 'media connected' report (common on flaky mobile networks) gets a spurious 409. This is a robustness/idempotency nit on a non-event-emitting milestone, not a data risk.
  - Fix: Treat connected idempotently: if current == Connected, return the existing row (200) instead of attempting the illegal Connected->Connected transition. Keep the Conflict only for genuinely incompatible states (terminal).
  - Verifier: minor — reported unverified

- ☐ **[LOW/presence/observability] WS GPS session has no OTel span around the per-fix persist/publish work**
  - `services/presence/src/api/ws.rs` 139 (session), 285 (handle_text)
  - Why: CLAUDE.md asks for an OTel span per request/handler/tx. The REST handlers carry #[tracing::instrument] and the booking consumer opens an info_span reparented on the producer trace, but the WS ingest path — which does the actual DB writes (upsert_location + insert_history) and the Redis publish per fix — runs only under tracing::info!/warn! events with no span. telemetry_middleware spans the upgrade GET, but that span ends at on_upgrade; the long-lived session and each handle_text persist/publish cycle are untraced, so a slow/erroring GPS write can't be correlated in Tempo the way the rest of the system can. This is the highest-volume write path in the service.
  - Fix: Open a session-scoped span (guard_id) at session() start and a short per-fix child span in handle_text around the tokio::join! of upsert/insert/publish, so the GPS write path is traceable and DB latency on the hot path is visible.
  - Verifier: minor — reported unverified

- ☐ **[LOW/presence/data-integrity] insert_history failure is logged-and-acked while the live row updates — silent gaps in the PDPA-retained history**
  - `services/presence/src/api/ws.rs` 330-352
  - Why: upsert_location, insert_history, and publish_gps run as three independent statements via tokio::join!. Only an upsert_location failure aborts the ack (correct — the ack carries the persisted recorded_at). An insert_history failure is logged at warn and otherwise swallowed (line 342-344), and the ack is still sent. So the live position can advance while the append-only location_history silently loses a point. For an operational live map that is acceptable, but location_history is the GPS-fraud/non-repudiation + PDPA-audited store (the file header and migration 0001 call it the SENSITIVE history), and history reads/playback will have gaps the client cannot distinguish from 'guard was stationary'. There is no metric/alert on the drop rate either.
  - Fix: At minimum emit a counter metric on insert_history failures (so a sustained DB problem dropping history is observable, not just a log line). If history completeness matters for fraud/audit, consider buffering and retrying the failed history row, or recording the gap explicitly, rather than dropping it silently behind a warn log.
  - Verifier: minor — reported unverified

- ☐ **[LOW/notification/correctness] send_notification / internal_push fail the whole request when FCM delivery errors, even though the log row was already written**
  - `services/notification/src/api/mod.rs` 118-147 (send_notification), 154-180 (internal_push), 38-56 (deliver)
  - Why: Both single-send paths insert_log() (commits the in-app notification) and then `deliver(...).await?` — propagating any FCM error. FcmPusher::push returns Err only when the OAuth2 access-token fetch fails (per-device send errors are already swallowed). So a transient Google token-exchange hiccup makes internal_push return 500 to the calling service AFTER the notification_logs row is already persisted. The caller (e.g. booking via service-JWT) will treat the push as failed and may retry, producing a DUPLICATE notification_logs row — there is no idempotency key on this internal path (unlike the event path's processed_events). The broadcast fan_out path deliberately treats push as best-effort (logs, continues); the single-send paths are inconsistent and not idempotent.
  - Fix: Make delivery best-effort on the single-send paths too: log the push error and still return success once the log row is committed (mirror fan_out). If at-least-once retry from callers is expected on /internal/push, add an idempotency key (e.g. an event_id/dedup header bound to processed_events) so a retry doesn't create a second log row.
  - Verifier: Verified against the code. In services/notification/src/api/mod.rs, both send_notification (lines 128-145) and internal_push (lines 160-178) call repo::insert_log(...).await? — a plain INSERT against the pool that commits immediately — and THEN deliver(...).await?, which propagates the Pusher error. In services/notification/src/fcm.rs, FcmPusher::push (284-295) returns Err only when access_token() (the OAuth2 token exchange) fails; per-device send errors in send_one (270-279) are logged via trac

- ☐ **[LOW/notification/correctness] Notification read filtering by client-supplied role is purely cosmetic and applies the same NULL-target-role fallback to count and to mark-read**
  - `services/notification/src/repo/mod.rs` 73-93 (list), 96-109 (unread_count), 136-154 (mark_all_as_read)
  - Why: The `role` query param is client-supplied (RoleQuery / ListNotificationsQuery) and is matched against payload->>'target_role' with an OR `payload->>'target_role' IS NULL` fallback in list, unread_count AND mark_all_as_read. All three are correctly scoped to user.user_id (no IDOR — good), so this is not an authz hole. But because the role filter is caller-chosen and untyped, a guard account that also has a customer payload row (target_role='customer') will, when calling with role=guard, still see/clear notifications whose target_role IS NULL. The asymmetry to watch: mark_all_as_read with role filtering will silently mark NULL-target notifications read for ANY role value passed, which can hide an unrelated-role notification from a dual-context user. This is a robustness/UX correctness nit, not a security issue, but worth confirming target_role is always populated (it is for event-mapped notifications via build_data, but NOT for admin send_notification, which stores the raw client payload with no target_role).
  - Fix: Confirm the intended semantics of the NULL fallback for mark_all_as_read; if role is meant to partition a dual-role user's feed, drop the `OR target_role IS NULL` from the mark-read mutation (or always stamp target_role on writes, including the admin send_notification path) so clearing one role's feed never clears the other's.
  - Verifier: minor — reported unverified

- ☐ **[LOW/notification/observability] internal_push logs caller + recipient user_id but the broadcast/internal push paths have no OTel span on the handler**
  - `services/notification/src/api/mod.rs` 154-180 (internal_push), 204-239 (fan_out), 399-417 (dispatch_due_broadcasts)
  - Why: The event consumer path is well-instrumented (handle_event reparents on traceparent, process_event has #[tracing::instrument]). The HTTP handlers rely on the shared telemetry_middleware for a request span, which is fine, but the broadcast fan_out and the timer-driven dispatch_due_broadcasts run state-changing per-recipient work (N inserts + N pushes) with only ad-hoc warn! logs and no span tying the per-recipient failures to a broadcast_id correlation. When a scheduled broadcast partially fails, the only signal is scattered tracing::warn! lines with no span/correlation_id, making it hard to answer 'did broadcast X reach everyone'. Per CLAUDE.md 'OTel span per request + event handler + DB transaction', the scheduler tick (not an HTTP request, so no middleware span) should carry its own span.
  - Fix: Add #[tracing::instrument(skip(state), fields(broadcast_id = %b.id, audience = %b.audience))] to fan_out and a span around each dispatch_due_broadcasts iteration so per-recipient warnings are correlated to the campaign and the timer-driven path (which gets no telemetry_middleware span) is traced.
  - Verifier: minor — reported unverified

- ☐ **[LOW/chat/correctness] mark_read writes the receipt under the caller's TOKEN role, not their role in this conversation**
  - `services/chat/src/api/mod.rs` 133-143 (mark_read) + repo/mod.rs:410-431
  - Why: mark_read gates by is_participant(conversation_id, user_id) (by user_id only, ws/api correct), but then writes the read receipt keyed by acting_role = the caller's TOKEN role (acting_role() returns &user.role for non-admins). The unread subquery in LIST_CONVERSATIONS_SQL joins read_receipts on (user_id, user_role=$2 acting_role). If a user's stored role in chat.participants for this conversation ever differs from their token role (the schema explicitly allows per-conversation roles — participants.user_role is per row), the receipt is written under a role that the unread query never reads back, so unread never clears for that conversation. In the current product a user holds a single role per account, so token-role == participant-role in practice and this does not bite today; flagged because the schema and the surrounding comments are built around the 'same user, different role per conversation' model, under which this is a silent unread-never-clears bug.
  - Fix: Derive the role for the receipt (and for the list/unread query) from the caller's chat.participants.user_role in that specific conversation rather than from the token role, mirroring how send_message already derives sender_role from membership. Then the receipt key and the unread predicate use the same authoritative per-conversation role.
  - Verifier: minor — reported unverified

- ☐ **[LOW/mobile/security] Biometric unlock does not reset the persisted PIN attempt counter**
  - `apps/mobile/lib/features/auth/pin_lock_screen.dart` 63-77
  - Why: On a successful biometric unlock, _authenticateBiometric calls sessionProvider.onUnlocked() but never resets the stored pin_attempts counter (only the PIN success path in PinService._verifyLocked does resetPinAttempts). pin_attempts is persisted across restarts (secure_store.dart writePinAttempts), so a user who fails the PIN a few times, then unlocks via Face ID, carries those wrong attempts into the next lock session — the next wrong PIN could trip the 5-wrong lockout (or count toward the 10-wrong wipe) earlier than the user expects. This is defensible as conservative defense-in-depth (a biometric success doesn't prove PIN knowledge), so it is a UX/consistency nit rather than a vulnerability — flagging for an explicit decision, not as a ship blocker.
  - Fix: If product intent is that a biometric unlock is equivalent to a correct PIN (it gates the same session), call ref.read(pinServiceProvider) to reset the attempt counter on biometric success — e.g. add a PinService.resetAttempts() that delegates to _store.resetPinAttempts() and invoke it in the `if (ok)` branch. If the conservative behavior is intended, add a one-line comment so it is not later 'fixed' by accident.
  - Verifier: minor — reported unverified

- ☐ **[LOW/web-admin/correctness] .env.example documents the wrong server env var (PGUARD_API_BASE_URL never listed) → silent localhost fallback in prod**
  - `apps/web-admin/.env.example` 11-16
  - Why: The code's server-side gateway base is read from process.env.PGUARD_API_BASE_URL in BOTH src/lib/session.ts:13 (`${process.env.PGUARD_API_BASE_URL ?? 'http://localhost:3000'}/v1`) and next.config.ts:14 (the /v1 rewrite destination). But .env.example never mentions PGUARD_API_BASE_URL — it instead documents API_GATEWAY_INTERNAL_URL (read nowhere in the codebase, confirmed by grep) and CSRF_HEADER_NAME (also read nowhere; the CSRF header is hard-coded to X-Requested-With in src/lib/api.ts:30). An operator who follows .env.example will set API_GATEWAY_INTERNAL_URL, leaving PGUARD_API_BASE_URL unset, so getServerSession() and the /v1 proxy rewrite both silently fall back to http://localhost:3000. In any real deployment that points the dashboard auth gate (GET /auth/me) and the entire same-origin API proxy at a non-existent localhost gateway → every session resolves to null → infinite redirect to /login, OR if a local gateway happens to listen, it talks to the wrong backend. This is a real config-drift bug, not cosmetic: the documented contract and the consumed contract disagree.
  - Fix: Replace API_GATEWAY_INTERNAL_URL with the actually-consumed PGUARD_API_BASE_URL (value WITHOUT a trailing /v1, since session.ts and next.config.ts both append /v1), and drop CSRF_HEADER_NAME (unused; header is hard-coded). Keep the names in code and example identical.
  - Verifier: VERIFIED as a real defect, but severity overstated; downgrade medium → low. The factual claims all hold: the server-side gateway base is read from process.env.PGUARD_API_BASE_URL in src/lib/session.ts:13 and next.config.ts:14 (both with a `http://localhost:3000` fallback), yet apps/web-admin/.env.example never lists PGUARD_API_BASE_URL. Instead it documents API_GATEWAY_INTERNAL_URL (.env.example:13) and CSRF_HEADER_NAME (:16) — repo-wide grep confirms BOTH are read nowhere in code. The CSRF head

- ☐ **[LOW/web-admin/web] NEXT_PUBLIC_API_BASE_URL example value steers the browser off-origin, breaking the first-party httpOnly-cookie model**
  - `apps/web-admin/.env.example` 6
  - Why: src/lib/api.ts:24 uses `process.env.NEXT_PUBLIC_API_BASE_URL ?? '/v1'` directly as the openapi-fetch baseUrl for the browser. The whole cookie design (api.ts header comment + next.config.ts rewrite) depends on the browser hitting SAME-ORIGIN '/v1' so the httpOnly access_token cookie stays first-party. But .env.example sets NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/v1, an absolute off-origin URL. If an operator copies the example as-is, browser auth calls go cross-origin to :8080, bypassing the Next same-origin proxy; the httpOnly cookie then requires CORS with credentials + SameSite=None to be sent at all (the api.ts comment even warns 'then CORS + SameSite must allow it'). The default '/v1' is correct and safe; the example value undermines it and invites a subtle auth-cookie breakage. Low because it is opt-in via env, but the example actively encourages the unsafe path.
  - Fix: In .env.example, leave NEXT_PUBLIC_API_BASE_URL unset/empty (or comment it out) so the safe same-origin '/v1' default applies, with a note that setting an absolute URL requires CORS+SameSite=None and is only for off-origin deployments.
  - Verifier: minor — reported unverified

- ☐ **[LOW/contracts+packages/api-contract] profile's /internal/profiles/recipients endpoint missing from profile.yaml**
  - `contracts/openapi/profile.yaml` registered at services/profile/src/main.rs:193 (handler api::internal_list_recipients, ServiceCaller-gated)
  - Why: GET /internal/profiles/recipients?audience={guards|all} is a live, service-JWT'd route (notification fan-out uses it) but is not in profile.yaml, which only documents /internal/guards. Same R4 source-of-truth gap as the export endpoint, lower impact (recipient list, not full PII). Auth is correct (ServiceCaller extractor present), so this is purely a contract-completeness miss.
  - Fix: Add the /internal/profiles/recipients operation (with the audience query enum and serviceAuth) to contracts/openapi/profile.yaml.
  - Verifier: minor — reported unverified

- ☐ **[LOW/contracts+packages/api-contract] AsyncAPI EnvelopeOf_BookingRef marks customer_id optional, but every booking-event consumer treats it as required**
  - `contracts/asyncapi/events.yaml` events.yaml:194-205 (required: [booking_id] only); consumer services/notification/src/domain/mapping.rs:78/85/92 reads uuid_field(payload,"customer_id")?
  - Why: GenericBookingEvent (declined/en_route/arrived/cancelled) uses EnvelopeOf_BookingRef whose required set is just [booking_id], so customer_id is contractually optional, and the generated BookingRef struct has customer_id: Option<Uuid>. In reality the producer (booking domain::events::payload) ALWAYS emits customer_id, and the notification mapper requires it to route (returns None — silently drops the notification — when absent). The contract is therefore looser than both the producer and the consumer contract-of-use. A future producer that trusted the schema and omitted customer_id would compile, pass the drift-lock (which uses payloads that include it), and silently break customer notifications for declined/en_route/arrived with no error. The drift-lock test only pins shapes that include the field, so it would not catch this. Not a current runtime bug (graceful None, no panic) — a latent contract/consumer mismatch.
  - Fix: Tighten EnvelopeOf_BookingRef to required: [booking_id, customer_id] (guard_id stays nullable/optional), regenerate, and re-run drift_lock. This makes the schema match what producers emit and consumers require, so a contract-driven producer can't drop the routing id.
  - Verifier: minor — reported unverified

- ☐ **[LOW/contracts+packages/observability] calling producer emits caller_id/callee_id/status not present in the AsyncAPI CallRef schema (acknowledged producer-vs-contract drift)**
  - `contracts/asyncapi/events.yaml` EnvelopeOf_CallRef events.yaml:297-307 (required: [call_id, booking_id]); producer services/calling/src/repo/mod.rs:336-341 adds caller_id/callee_id/status
  - Why: The calling service publishes pguard.events.calling.* with caller_id, callee_id, and status fields that are absent from the contract's EnvelopeOf_CallRef. The open object schema tolerates the extras and the generated CallRef locks only the documented subset (forward-compatible, and drift_lock.rs:166-193 explicitly documents this), so nothing breaks today. But it means the AsyncAPI contract under-describes the real wire payload: a future calling consumer that needs caller_id/callee_id has no contract to bind to, and the asyncapi 'source of truth' is incomplete for this context. Already flagged in code comments as a reconcile follow-up.
  - Fix: Either add caller_id (uuid), callee_id (uuid), and status (enum) to EnvelopeOf_CallRef in events.yaml and regenerate, or trim the producer to the documented subset. Pick one so contract and producer converge.
  - Verifier: minor — reported unverified

- ☐ **[NIT/rating/performance] Admin list recomputes a full-table COUNT/AVG over guard_reviews on every call**
  - `services/rating/src/repo/mod.rs` 297-302
  - Why: list_admin_reviews issues an unfiltered 'SELECT COUNT(*), COUNT(*) FILTER (...), AVG(overall_rating) FROM rating.guard_reviews' on every admin list request (a sequential scan of the whole table, independent of the page filters). This is intentional v1-parity (the dashboard stat cards reflect the whole dataset, documented at lines 222-224 and in the OpenAPI description) and runs on the read replica, so it is not a correctness bug. Flagging only as a future scaling note: as the reviews table grows this full-table aggregate per admin pageview becomes the dominant cost. Consider a periodically-refreshed materialized aggregate or a cached count if the admin console paginates frequently.
  - Fix: Optional/future: back the unfiltered stats with a small cached/materialized aggregate (refreshed on submit/visibility change) instead of a per-request full-table COUNT/AVG, if admin-list traffic grows.
  - Verifier: minor — reported unverified

- ☐ **[NIT/contracts+packages/api-contract] contracts/README claims generated clients are gitignored, but they are committed**
  - `contracts/README.md` README.md:3-4 ('generated output is gitignored (commit only the specs)') vs committed apps/web-admin/src/api/generated/*.ts and apps/mobile/lib/api/generated/*/lib/*.dart
  - Why: The README states generated output is gitignored and only specs are committed, yet the TS and Dart generated clients are tracked in the repo (and are correctly co-committed with their specs — verified via git log, so they are NOT stale). The doc is simply out of date relative to the chosen workflow. Harmless, but a reader following the README would expect to run codegen to get the clients and be confused that they are versioned. Pure documentation inconsistency.
  - Fix: Update contracts/README.md to state that generated clients are committed (so reviewers can see API diffs) and regenerated via tooling/codegen/generate.sh, reconciling it with tooling/codegen/README.md.
  - Verifier: minor — reported unverified
