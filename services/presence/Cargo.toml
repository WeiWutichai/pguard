[package]
name = "pguard-presence"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "pguard-presence"
path = "src/main.rs"

[dependencies]
axum = { workspace = true }
tokio = { workspace = true }
serde_json = { workspace = true }
sqlx = { workspace = true }              # location_history + retention purge
uuid = { workspace = true }
chrono = { workspace = true }            # retention cutoff (now - N days)
tracing = { workspace = true }
anyhow = { workspace = true }
shared = { workspace = true }            # DatabaseConfig + create_pool
observability = { workspace = true }
